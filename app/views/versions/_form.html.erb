<%= form_with(model: version) do |form| %>
  <% if version.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(version.errors.count, "error") %> prohibited this version from being saved:</h2>

      <ul>
        <% version.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :version_number, style: "display: block" %>
    <%= form.text_field :version_number %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :part_id, style: "display: block" %>
    <%= form.text_field :part_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
